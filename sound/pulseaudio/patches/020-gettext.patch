--- a/meson.build
+++ b/meson.build
@@ -300,11 +300,7 @@ if cc.has_header_symbol('sys/syscall.h',
   cdata.set('HAVE_MEMFD', 1)
 endif
 
-if cc.has_function('dgettext')
-  libintl_dep = []
-else
   libintl_dep = cc.find_library('intl')
-endif
 
 # Symbols
 
