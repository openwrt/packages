--- a/cmd/meson.build
+++ b/cmd/meson.build
@@ -22,6 +22,6 @@ executable(meson.project_name(),
   'alg.c',
   'fmt.c',
   dependencies: jansson,
-  link_with: libjose,
+  link_whole: libjose,
   install: true
 )
