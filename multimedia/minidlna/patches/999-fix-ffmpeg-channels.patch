From 5aed88a7ec99defa06f26b60d006940aba03d0fd Mon Sep 17 00:00:00 2001
From: Marcin Maj <marcinmajsc@gmail.com>
Date: Wed, 23 Sep 2026 20:09:07 +0200
Subject: [PATCH] Fix audio channel count access with FFmpeg 7 and later

---
 libav.h | 4 ++++
 1 file changed, 4 insertions(+)

--- a/libav.h
+++ b/libav.h
@@ -174,7 +174,11 @@ lav_get_interlaced(AVStream *s)
 #define lav_codec_tag(s) s->codecpar->codec_tag
 #define lav_sample_rate(s) s->codecpar->sample_rate
 #define lav_bit_rate(s) s->codecpar->bit_rate
+#if LIBAVCODEC_VERSION_MAJOR >= 61
+#define lav_channels(s) s->codecpar->ch_layout.nb_channels
+#else
 #define lav_channels(s) s->codecpar->channels
+#endif
 #define lav_width(s) s->codecpar->width
 #define lav_height(s) s->codecpar->height
 #define lav_profile(s) s->codecpar->profile
