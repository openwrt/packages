#!/bin/sh /etc/rc.common

USE_PROCD=1
START=95
STOP=10

PROG=/usr/bin/mollysocket
CONF=/etc/mollysocket.conf

start_service() {
	procd_open_instance
	procd_set_param command "$PROG" server
	procd_set_param env MOLLY_CONF="$CONF"
	procd_set_param respawn
	procd_close_instance
}
